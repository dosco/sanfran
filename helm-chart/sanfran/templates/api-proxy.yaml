## API Proxy Layer

apiVersion: v1
kind: Service
metadata:
  name: sanfran-api-proxy-service
spec:
  type: {{ .Values.k8s.services.type }}
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: sanfran-api-proxy

---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: sanfran-api-proxy
spec:
  selector:
    matchLabels:
      app: sanfran-api-proxy
  replicas:  {{ .Values.apiProxy.instances }}
  template:
    metadata:
      labels:
        app: sanfran-api-proxy
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      containers:
        - name: sanfran-api-proxy
          image: dosco/sanfran-api-proxy:{{ .Values.apiProxy.tag }}
          securityContext:
            #allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: SANFRAN_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
